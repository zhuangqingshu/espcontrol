# =============================================================================
# TIME - NTP/Home Assistant time sources and timezone selector
# =============================================================================
# SNTP drives the on-screen clock when network NTP is available. Home Assistant
# time remains enabled as a fallback. The timezone selector provides approximate
# lat/lon for on-device sunrise/sunset calculation (sun_calc.h).
# =============================================================================

globals:
  - id: clock_format_12h
    type: bool
    initial_value: 'false'

esphome:
  on_boot:
    priority: -100
    then:
      - lambda: |-
          id(clock_format_12h) = id(clock_format_select).current_option() == "12h";
          std::string timezone_option = id(timezone_select).current_option();
          apply_configured_timezone(timezone_option);
          set_espcontrol_language(id(language_select).current_option());
          set_display_temperature_unit(id(temperature_unit_select).current_option(),
                                       timezone_option);
          id(screen_active_timezone_sensor).publish_state(effective_timezone_option(timezone_option));
      - script.execute: network_setup_text_refresh
      - script.execute: ha_setup_text_refresh
      - script.execute: ha_actions_text_refresh
      - script.execute: button_setup_text_refresh

# Ask Home Assistant time is scheduled centrally by core_infra.yaml after the
# native API handshake has completed.  Keeping it there prevents a second
# connection hook from running work before the protocol Hello response.
# ---------------------------------------------------------------------------
# Timezone selector (drives sunrise/sunset calculation via sun_calc.h)
# ---------------------------------------------------------------------------
select:
  - platform: template
    name: "${entity_screen_timezone}"
    internal: true
    disabled_by_default: true
    id: timezone_select
    entity_category: config
    icon: "mdi:map-clock"
    optimistic: true
    restore_value: true
    initial_option: "Auto (Home Assistant)"
    options:
      - "Pacific/Midway (GMT-11)"
      - "Pacific/Pago_Pago (GMT-11)"
      - "Pacific/Honolulu (GMT-10)"
      - "America/Adak (GMT-10)"
      - "America/Anchorage (GMT-9)"
      - "America/Juneau (GMT-9)"
      - "America/Los_Angeles (GMT-8)"
      - "America/Vancouver (GMT-8)"
      - "America/Tijuana (GMT-8)"
      - "America/Denver (GMT-7)"
      - "America/Phoenix (GMT-7)"
      - "America/Edmonton (GMT-7)"
      - "America/Boise (GMT-7)"
      - "America/Chicago (GMT-6)"
      - "America/Mexico_City (GMT-6)"
      - "America/Winnipeg (GMT-6)"
      - "America/Guatemala (GMT-6)"
      - "America/Costa_Rica (GMT-6)"
      - "America/New_York (GMT-5)"
      - "America/Toronto (GMT-5)"
      - "America/Detroit (GMT-5)"
      - "America/Havana (GMT-5)"
      - "America/Bogota (GMT-5)"
      - "America/Lima (GMT-5)"
      - "America/Jamaica (GMT-5)"
      - "America/Panama (GMT-5)"
      - "America/Halifax (GMT-4)"
      - "America/Caracas (GMT-4)"
      - "America/Santiago (GMT-4)"
      - "America/La_Paz (GMT-4)"
      - "America/Manaus (GMT-4)"
      - "America/Barbados (GMT-4)"
      - "America/Puerto_Rico (GMT-4)"
      - "America/Santo_Domingo (GMT-4)"
      - "America/St_Johns (GMT-3:30)"
      - "America/Sao_Paulo (GMT-3)"
      - "America/Argentina/Buenos_Aires (GMT-3)"
      - "America/Montevideo (GMT-3)"
      - "America/Paramaribo (GMT-3)"
      - "Atlantic/South_Georgia (GMT-2)"
      - "Atlantic/Azores (GMT-1)"
      - "Atlantic/Cape_Verde (GMT-1)"
      - "UTC (GMT+0)"
      - "Europe/London (GMT+0)"
      - "Europe/Dublin (GMT+0)"
      - "Europe/Lisbon (GMT+0)"
      - "Africa/Casablanca (GMT+1)"
      - "Africa/Accra (GMT+0)"
      - "Atlantic/Reykjavik (GMT+0)"
      - "Europe/Paris (GMT+1)"
      - "Europe/Berlin (GMT+1)"
      - "Europe/Rome (GMT+1)"
      - "Europe/Madrid (GMT+1)"
      - "Europe/Amsterdam (GMT+1)"
      - "Europe/Brussels (GMT+1)"
      - "Europe/Vienna (GMT+1)"
      - "Europe/Zurich (GMT+1)"
      - "Europe/Stockholm (GMT+1)"
      - "Europe/Oslo (GMT+1)"
      - "Europe/Copenhagen (GMT+1)"
      - "Europe/Warsaw (GMT+1)"
      - "Europe/Prague (GMT+1)"
      - "Europe/Budapest (GMT+1)"
      - "Europe/Belgrade (GMT+1)"
      - "Africa/Lagos (GMT+1)"
      - "Africa/Tunis (GMT+1)"
      - "Africa/Cairo (GMT+2)"
      - "Europe/Athens (GMT+2)"
      - "Europe/Bucharest (GMT+2)"
      - "Europe/Helsinki (GMT+2)"
      - "Europe/Kyiv (GMT+2)"
      - "Europe/Istanbul (GMT+3)"
      - "Africa/Johannesburg (GMT+2)"
      - "Africa/Nairobi (GMT+3)"
      - "Asia/Jerusalem (GMT+2)"
      - "Asia/Amman (GMT+3)"
      - "Asia/Beirut (GMT+2)"
      - "Europe/Moscow (GMT+3)"
      - "Asia/Baghdad (GMT+3)"
      - "Asia/Riyadh (GMT+3)"
      - "Asia/Kuwait (GMT+3)"
      - "Asia/Qatar (GMT+3)"
      - "Africa/Addis_Ababa (GMT+3)"
      - "Asia/Tehran (GMT+3:30)"
      - "Asia/Dubai (GMT+4)"
      - "Asia/Muscat (GMT+4)"
      - "Asia/Baku (GMT+4)"
      - "Asia/Tbilisi (GMT+4)"
      - "Indian/Mauritius (GMT+4)"
      - "Asia/Kabul (GMT+4:30)"
      - "Asia/Karachi (GMT+5)"
      - "Asia/Tashkent (GMT+5)"
      - "Asia/Yekaterinburg (GMT+5)"
      - "Asia/Kolkata (GMT+5:30)"
      - "Asia/Colombo (GMT+5:30)"
      - "Asia/Kathmandu (GMT+5:45)"
      - "Asia/Dhaka (GMT+6)"
      - "Asia/Almaty (GMT+6)"
      - "Asia/Rangoon (GMT+6:30)"
      - "Asia/Bangkok (GMT+7)"
      - "Asia/Jakarta (GMT+7)"
      - "Asia/Ho_Chi_Minh (GMT+7)"
      - "Asia/Singapore (GMT+8)"
      - "Asia/Kuala_Lumpur (GMT+8)"
      - "Asia/Shanghai (GMT+8)"
      - "Asia/Hong_Kong (GMT+8)"
      - "Asia/Taipei (GMT+8)"
      - "Asia/Manila (GMT+8)"
      - "Australia/Perth (GMT+8)"
      - "Asia/Tokyo (GMT+9)"
      - "Asia/Seoul (GMT+9)"
      - "Asia/Pyongyang (GMT+9)"
      - "Australia/Adelaide (GMT+9:30)"
      - "Australia/Darwin (GMT+9:30)"
      - "Australia/Sydney (GMT+10)"
      - "Australia/Melbourne (GMT+10)"
      - "Australia/Brisbane (GMT+10)"
      - "Australia/Hobart (GMT+10)"
      - "Pacific/Guam (GMT+10)"
      - "Pacific/Port_Moresby (GMT+10)"
      - "Asia/Vladivostok (GMT+10)"
      - "Pacific/Noumea (GMT+11)"
      - "Pacific/Norfolk (GMT+11)"
      - "Asia/Magadan (GMT+11)"
      - "Pacific/Auckland (GMT+12)"
      - "Pacific/Fiji (GMT+12)"
      - "Pacific/Chatham (GMT+12:45)"
      - "Pacific/Tongatapu (GMT+13)"
      - "Pacific/Apia (GMT+13)"
      - "Pacific/Kiritimati (GMT+14)"
      - "Auto (Home Assistant)"
    web_server:
      sorting_group_id: sg_brightness
    on_value:
      then:
        - lambda: |-
            std::string timezone_option(x.c_str(), x.size());
            const char *posix = apply_configured_timezone(timezone_option);
            if (posix == nullptr) {
              if (ha_api_state_connected()) id(homeassistant_time).update();
            }
            set_display_temperature_unit(id(temperature_unit_select).current_option(),
                                         timezone_option);
            id(screen_active_timezone_sensor).publish_state(effective_timezone_option(timezone_option));
            refresh_temperature_unit_labels();
            refresh_clock_bar_temperature_label_values(
              id(main_page)->obj,
              clock_bar_should_show(
                id(clock_bar_enabled).state,
                id(main_page)->obj,
                id(espcontrol_app).display().current_mode(),
              id(espcontrol_app).display().target_schedule_inactive()),
              id(indoor_temp_enable).state,
              id(outdoor_temp_enable).state,
              id(indoor_temp), id(outdoor_temp));
        - script.execute: timezone_after_homeassistant_time_sync

  - platform: template
    name: "${entity_screen_clock_format}"
    internal: true
    disabled_by_default: true
    id: clock_format_select
    entity_category: config
    icon: "mdi:clock-outline"
    optimistic: true
    restore_value: true
    initial_option: "24h"
    options:
      - "12h"
      - "24h"
    web_server:
      sorting_group_id: sg_brightness
    on_value:
      then:
        - lambda: 'id(clock_format_12h) = id(clock_format_select).current_option() == "12h";'
        - script.execute: time_update
        - script.execute: backlight_recalc_sunrise_sunset

  - platform: template
    name: "${entity_screen_language}"
    internal: true
    disabled_by_default: true
    id: language_select
    entity_category: config
    icon: "mdi:translate"
    optimistic: true
    restore_value: true
    initial_option: "en"
    options:
      - "en"
      - "cs"
      - "da"
      - "de"
      - "es"
      - "fi"
      - "fr"
      - "he"
      - "hu"
      - "it"
      - "nb"
      - "nl"
      - "pl"
      - "pt"
      - "pt-br"
      - "ro"
      - "ru"
      - "sk"
      - "sl"
      - "sv"
      - "tr"
      - "uk"
      - "zh"
    web_server:
      sorting_group_id: sg_brightness
    on_value:
      then:
        - lambda: |-
            set_espcontrol_language(id(language_select).current_option());
            refresh_calendar_cards();
        - script.execute: network_setup_text_refresh
        - script.execute: ha_setup_text_refresh
        - script.execute: ha_actions_text_refresh
        - script.execute: button_setup_text_refresh
        - script.execute: refresh_button_grid
        - script.execute: network_status_refresh

# ---------------------------------------------------------------------------
# NTP server settings and date source for Date cards
# ---------------------------------------------------------------------------
text:
  - platform: template
    name: "${entity_screen_ntp_server_1}"
    internal: true
    id: ntp_server_1
    optimistic: true
    restore_value: true
    initial_value: "0.pool.ntp.org"
    min_length: 0
    max_length: 100
    mode: text
    entity_category: config
    icon: "mdi:server-network"
    update_interval: never
    web_server:
      sorting_group_id: sg_brightness
    on_value:
      then:
        - script.execute: ntp_servers_apply_after_network

  - platform: template
    name: "${entity_screen_ntp_server_2}"
    internal: true
    id: ntp_server_2
    optimistic: true
    restore_value: true
    initial_value: "1.pool.ntp.org"
    min_length: 0
    max_length: 100
    mode: text
    entity_category: config
    icon: "mdi:server-network"
    update_interval: never
    web_server:
      sorting_group_id: sg_brightness
    on_value:
      then:
        - script.execute: ntp_servers_apply_after_network

  - platform: template
    name: "${entity_screen_ntp_server_3}"
    internal: true
    id: ntp_server_3
    optimistic: true
    restore_value: true
    initial_value: "2.pool.ntp.org"
    min_length: 0
    max_length: 100
    mode: text
    entity_category: config
    icon: "mdi:server-network"
    update_interval: never
    web_server:
      sorting_group_id: sg_brightness
    on_value:
      then:
        - script.execute: ntp_servers_apply_after_network

text_sensor:
  # Diagnostic value published from the active time source. This makes
  # the date visible as an ESPHome state and gives Date cards a clear source.
  - platform: template
    name: "Screen: Date"
    id: screen_date_sensor
    entity_category: diagnostic
    icon: "mdi:calendar"
    update_interval: never
    web_server:
      sorting_group_id: sg_brightness

  - platform: template
    name: "${entity_screen_active_timezone}"
    id: screen_active_timezone_sensor
    entity_category: diagnostic
    icon: "mdi:map-clock-outline"
    update_interval: never
    web_server:
      sorting_group_id: sg_brightness

  # Optional direct Home Assistant Date & Time integration source. When
  # Home Assistant has sensor.date, use it immediately; otherwise the active
  # panel time source below remains the fallback.
  - platform: homeassistant
    id: homeassistant_date_sensor
    entity_id: sensor.date
    internal: true
    on_value:
      then:
        - lambda: |-
            if (update_calendar_cards_from_date_text(x)) {
              id(screen_date_sensor).publish_state(x);
            }

# ---------------------------------------------------------------------------
# SNTP primary time source and Home Assistant fallback. SNTP sync refreshes
# sunrise/sunset; Home Assistant sync only refreshes the clock/schedule.
# ---------------------------------------------------------------------------
time:
  - platform: sntp
    id: panel_time
    servers:
      - "0.pool.ntp.org"
      - "1.pool.ntp.org"
      - "2.pool.ntp.org"
    on_time_sync:
      - script.execute: time_update
      - script.execute: backlight_recalc_sunrise_sunset
      - script.execute: screen_schedule_check
    on_time:
      - minutes: '*'
        seconds: 0
        then:
          - script.execute: time_update

  - platform: homeassistant
    id: homeassistant_time
    update_interval: 1min
    on_time_sync:
      - script.execute: timezone_after_homeassistant_time_sync
    on_time:
      - minutes: '*'
        seconds: 0
        then:
          - script.execute: time_update

script:
  - id: ntp_servers_apply_after_network
    mode: restart
    then:
      - delay: 2s
      - script.execute: ntp_servers_apply

  - id: ntp_servers_apply
    mode: restart
    then:
      - lambda: |-
          apply_ntp_servers(id(ntp_server_1).state,
                            id(ntp_server_2).state,
                            id(ntp_server_3).state);

  - id: timezone_after_homeassistant_time_sync
    mode: restart
    then:
      - delay: 1200ms
      - lambda: |-
          std::string timezone_option = id(timezone_select).current_option();
          const char *posix = apply_configured_timezone(timezone_option);
          set_display_temperature_unit(id(temperature_unit_select).current_option(),
                                       timezone_option);
          std::string active_timezone = effective_timezone_option(timezone_option);
          id(screen_active_timezone_sensor).publish_state(active_timezone);
          ESP_LOGI("timezone", "Configured timezone: %s; active timezone: %s",
                   timezone_option.c_str(), active_timezone.c_str());
          refresh_temperature_unit_labels();
          refresh_clock_bar_temperature_label_values(
            id(main_page)->obj,
            clock_bar_should_show(
              id(clock_bar_enabled).state,
              id(main_page)->obj,
              id(espcontrol_app).display().current_mode(),
              id(espcontrol_app).display().target_schedule_inactive()),
            id(indoor_temp_enable).state,
            id(outdoor_temp_enable).state,
            id(indoor_temp), id(outdoor_temp));
      - script.execute: time_update
      - script.execute: backlight_recalc_sunrise_sunset
      - script.execute: screen_schedule_check

  - id: time_update
    then:
      - lvgl.label.update:
          id: display_time
          text: !lambda |-
            static char time_buf[8];
            auto now = panel_time_or_fallback(id(panel_time).now(),
                                              id(homeassistant_time).now());
            if (!now.is_valid()) return "--:--";
            bool use_12h = id(clock_format_12h);
            format_clock_time_without_suffix(time_buf, sizeof(time_buf),
                                             now.hour, now.minute, use_12h);
            return time_buf;
      - lambda: |-
          auto now = panel_time_or_fallback(id(panel_time).now(),
                                            id(homeassistant_time).now());
          bool use_12h = id(clock_format_12h);
          if (now.is_valid()) {
            static char date_buf[16];
            snprintf(date_buf, sizeof(date_buf), "%04d-%02d-%02d",
                     now.year, now.month, now.day_of_month);
            id(screen_date_sensor).publish_state(date_buf);
            update_calendar_cards(true, now.day_of_month, now.month);
            update_calendar_cards_time(
                true, now.day_of_month, now.month, now.hour, now.minute, use_12h);
            update_timezone_cards(true, now.timestamp,
                                  id(timezone_select).current_option(), use_12h);
          } else if (!calendar_date_valid()) {
            if (ha_api_state_connected()) id(homeassistant_time).update();
            id(screen_date_sensor).publish_state("");
            update_calendar_cards(false, 0, 0);
            update_calendar_cards_time(false, 0, 0, 0, 0, false);
            update_timezone_cards(false, 0, id(timezone_select).current_option(), use_12h);
          } else {
            if (ha_api_state_connected()) id(homeassistant_time).update();
            update_calendar_cards_time(false, 0, 0, 0, 0, false);
            update_timezone_cards(false, 0, id(timezone_select).current_option(), use_12h);
          }
